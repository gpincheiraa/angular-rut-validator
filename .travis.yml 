language: node_js
sudo: false
node_js:
  - 0.10
script: node_modules/karma/bin/karma start karma.conf.js –single-run
before_script:
  - npm install 
after_script: NODE_ENV=cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js && rm -rf coverage

notifications:

  email:
    recipients:
      – g.pincheira.a@gmail.com

on_failure: always